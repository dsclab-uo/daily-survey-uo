{
  "name": "daily-survey-push-server",
  "version": "1.0.0",
  "description": "Sends scheduled Web Push notifications for the Daily Survey PWA, even when the app is closed.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate-keys": "node generate-vapid-keys.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18"
  }
}
